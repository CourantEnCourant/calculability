def tokenize(ts_script: str) -> list[str]:
	"""
	Strip all lines of commentary
	return a list of instructions
	"""
	return
	
	
def check_syntaxe(ts_script: list[str]) -> bool:
	"""
	Returns True if program is valid, False (or raise) if not.
	Check if a tokenized ts_script is a valid program of not
	There are a few possible errors. The script needs to be ended, parenthesis have to match, a loop without ending condition, etc.
	Feel free to elaborate if you have other errors in mind.
	It will be nice if we can have a different message for different kind of problem.
	"""
	return
	